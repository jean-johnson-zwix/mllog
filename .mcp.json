{
  "mcpServers": {
    "mllog": {
      "command": "uv",
      "args": [
        "run", "--no-project", "--python", ">=3.10", "python", "-c",
        "import os,runpy;runpy.run_path(os.path.join(os.environ['CLAUDE_PLUGIN_ROOT'],'mcp_launcher.py'),run_name='__main__')"
      ]
    }
  }
}
