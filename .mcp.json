{
  "mcpServers": {
    "mllog": {
      "command": "uv",
      "args": [
        "run", "--no-project", "--python", ">=3.10", "python", "-c",
        "import glob,os,re,runpy;k=lambda p:[int(n) for n in re.findall(r'\\d+',p.split(os.sep)[-2])];g=glob.glob(os.path.expanduser('~/.claude/plugins/cache/*/mllog/*/mcp_launcher.py'));runpy.run_path(max(g,key=k),run_name='__main__')"
      ]
    }
  }
}
